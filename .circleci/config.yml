version: '2.1'
orbs:
  slack: circleci/slack@4.1
jobs:
  notify:
    docker:
      - image: circleci/python:3.6.4
    steps:
      - slack/notify:
          template: basic_success_1
          event: always
workflows:
  send-notification:
    jobs:
      - notify:
          context: SLACK_SECRETS
          
         


